"""Optional building blocks for wiring an LLM up to a Registrar as a live
tool-calling agent, not just a text-completion caller -- see
`emc2p.agents.keyed_subagent`. Requires the `agents` extra (`litellm`).
"""
